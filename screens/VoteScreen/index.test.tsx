import { render, screen } from "@testing-library/react-native";
import VoteScreen from "./index";

describe("VoteScreen", () => {
  it("renders correctly", () => {
    //render(<VoteScreen />);
  });
});
